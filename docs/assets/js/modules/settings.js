import { saveStoreSettings } from '../services/data-service.js?v=2.1.3';
import { changeOwnPassword } from '../services/auth-service.js?v=2.1.3';
import { state } from '../core/state.js?v=2.1.3';
import { getErrorMessage, escapeHTML } from '../core/utils.js?v=2.1.3';
import { toast, setButtonLoading, pageLoading } from '../core/ui.js?v=2.1.3';

export async function renderSettings(container){container.innerHTML=`<section class="page">${pageLoading('Menyiapkan pengaturan…')}</section>`;draw(container)}
function draw(container){const store=state.store||{};container.innerHTML=`<section class="page"><header class="page-head"><div><span class="eyebrow">Konfigurasi Aplikasi</span><h2>Pengaturan Toko</h2><p>Identitas pada struk, aturan pajak, dan keamanan akun.</p></div></header><div class="dashboard-grid"><article class="card"><div class="card__head"><div><h3>Profil Toko</h3><p>Informasi ini tampil pada struk dan halaman aplikasi.</p></div></div><div class="card__body"><form id="store-form"><div class="form-grid"><div class="field field--full"><label>Nama Toko *</label><input name="name" required value="${escapeHTML(store.name||'Toko Emas Hidayah')}"></div><div class="field field--full"><label>Tagline</label><input name="tagline" value="${escapeHTML(store.tagline||'')}"></div><div class="field"><label>Telepon</label><input name="phone" value="${escapeHTML(store.phone||'')}"></div><div class="field"><label>WhatsApp</label><input name="whatsapp" value="${escapeHTML(store.whatsapp||'')}"></div><div class="field field--full"><label>Alamat</label><textarea name="address">${escapeHTML(store.address||'')}</textarea></div><div class="field"><label>Kota</label><input name="city" value="${escapeHTML(store.city||'')}"></div><div class="field"><label>Pajak Default (%)</label><input type="number" step="0.01" name="taxPercent" value="${store.taxPercent||0}"></div><div class="field"><label>Stok Minimum Default</label><input type="number" step="0.001" name="lowStockDefault" value="${store.lowStockDefault||1}"></div><div class="field field--full"><label>Footer Struk</label><textarea name="receiptFooter">${escapeHTML(store.receiptFooter||'')}</textarea></div></div><button class="button button--gold" type="submit" style="margin-top:18px">Simpan Profil Toko</button></form></div></article><div class="dashboard-stack"><article class="card"><div class="card__head"><div><h3>Keamanan Akun</h3><p>Ganti kata sandi akun Anda.</p></div></div><div class="card__body"><form id="password-form"><div class="field"><label>Kata Sandi Saat Ini</label><input type="password" name="currentPassword" required></div><div class="field" style="margin-top:12px"><label>Kata Sandi Baru</label><input type="password" name="newPassword" minlength="6" required></div><div class="field" style="margin-top:12px"><label>Ulangi Kata Sandi Baru</label><input type="password" name="confirmPassword" minlength="6" required></div><button class="button button--dark button--block" type="submit" style="margin-top:16px">Ganti Kata Sandi</button></form></div></article><article class="card"><div class="card__head"><div><h3>Informasi Sistem</h3></div></div><div class="card__body"><div class="metric-list"><div class="metric-row"><span>Project Firebase</span><strong>tokoemas-79e07</strong></div><div class="metric-row"><span>Database</span><strong>Cloud Firestore</strong></div><div class="metric-row"><span>Versi Aplikasi</span><strong>2.1.3</strong></div><div class="metric-row"><span>Mode Aplikasi</span><strong>PWA Responsif</strong></div></div></div></article></div></div></section>`;
const storeForm=container.querySelector('#store-form');storeForm.addEventListener('submit',async event=>{event.preventDefault();const button=storeForm.querySelector('[type=submit]');setButtonLoading(button,true,'Menyimpan…');try{const fd=new FormData(storeForm);const payload={name:fd.get('name'),tagline:fd.get('tagline'),phone:fd.get('phone'),whatsapp:fd.get('whatsapp'),address:fd.get('address'),city:fd.get('city'),taxPercent:Number(fd.get('taxPercent'))||0,lowStockDefault:Number(fd.get('lowStockDefault'))||1,receiptFooter:fd.get('receiptFooter')};await saveStoreSettings(payload);state.store={...state.store,...payload};toast('Profil toko berhasil diperbarui.');setButtonLoading(button,false)}catch(error){toast(getErrorMessage(error),'error');setButtonLoading(button,false)}});
const passForm=container.querySelector('#password-form');passForm.addEventListener('submit',async event=>{event.preventDefault();if(passForm.elements.newPassword.value!==passForm.elements.confirmPassword.value)return toast('Konfirmasi kata sandi tidak sama.','warning');const button=passForm.querySelector('[type=submit]');setButtonLoading(button,true,'Mengubah…');try{await changeOwnPassword(passForm.elements.currentPassword.value,passForm.elements.newPassword.value);passForm.reset();toast('Kata sandi berhasil diubah.');setButtonLoading(button,false)}catch(error){toast(getErrorMessage(error),'error');setButtonLoading(button,false)}})}
