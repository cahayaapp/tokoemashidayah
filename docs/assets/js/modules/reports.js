import { listSales, listPurchases, listBuybacks, listExpenses, voidSale } from '../services/data-service.js?v=2.1.3';
import { canManage } from '../core/state.js?v=2.1.3';
import { formatRupiah, formatDateTime, formatDate, formatNumber, sum, dateInputValue, downloadCSV, getErrorMessage, escapeHTML } from '../core/utils.js?v=2.1.3';
import { pageLoading, tableEmpty, badge, confirmDialog, toast, setButtonLoading } from '../core/ui.js?v=2.1.3';

let startDate;
let endDate;
let sales=[];let purchases=[];let buybacks=[];let expenses=[];let tab='sales';

export async function renderReports(container){const now=new Date();startDate=new Date(now.getFullYear(),now.getMonth(),1);endDate=new Date(now.getFullYear(),now.getMonth()+1,0,23,59,59,999);container.innerHTML=`<section class="page">${pageLoading('Menyusun laporan…')}</section>`;await loadData();draw(container)}
async function loadData(){[sales,purchases,buybacks,expenses]=await Promise.all([listSales({start:startDate,end:endDate,max:3000}),listPurchases({start:startDate,end:endDate,max:3000}),listBuybacks({start:startDate,end:endDate,max:3000}),listExpenses({start:startDate,end:endDate,max:3000})])}
function metrics(){const validSales=sales.filter(item=>item.status!=='void');const revenue=sum(validSales,item=>item.grandTotal);const hpp=sum(validSales,sale=>sum(sale.lines||[],line=>line.estimatedCost));const purchaseTotal=sum(purchases,item=>item.total);const buybackTotal=sum(buybacks,item=>item.totalPayout);const expenseTotal=sum(expenses,item=>item.amount);return{revenue,hpp,purchaseTotal,buybackTotal,expenseTotal,profit:revenue-hpp-expenseTotal,cashFlow:revenue-purchaseTotal-buybackTotal-expenseTotal,validSales}}
function draw(container){const m=metrics();container.innerHTML=`<section class="page print-report"><header class="page-head"><div><span class="eyebrow">Analitik & Rekapitulasi</span><h2>Laporan Toko</h2><p>Ringkasan penjualan, arus kas, laba estimasi, dan rincian transaksi.</p></div><div class="page-actions"><button class="button button--outline" id="export-report">⇩ Ekspor CSV</button><button class="button button--dark" id="print-report">Cetak</button></div></header><article class="card no-print" style="margin-bottom:16px"><div class="card__body"><div class="report-filter"><div class="field"><label>Tanggal Mulai</label><input type="date" id="report-start" value="${dateInputValue(startDate)}"></div><div class="field"><label>Tanggal Akhir</label><input type="date" id="report-end" value="${dateInputValue(endDate)}"></div><div class="field"><label>Jenis Laporan</label><select id="report-tab"><option value="sales" ${tab==='sales'?'selected':''}>Penjualan</option><option value="purchases" ${tab==='purchases'?'selected':''}>Pembelian</option><option value="buybacks" ${tab==='buybacks'?'selected':''}>Buyback</option><option value="expenses" ${tab==='expenses'?'selected':''}>Pengeluaran</option></select></div><div></div><button class="button button--gold" id="apply-report">Tampilkan</button></div></div></article><div class="report-summary"><div class="mini-stat"><span>Omzet</span><strong>${formatRupiah(m.revenue)}</strong></div><div class="mini-stat"><span>Estimasi HPP</span><strong>${formatRupiah(m.hpp)}</strong></div><div class="mini-stat"><span>Pengeluaran</span><strong>${formatRupiah(m.expenseTotal)}</strong></div><div class="mini-stat"><span>Estimasi Laba</span><strong>${formatRupiah(m.profit)}</strong></div><div class="mini-stat"><span>Arus Kas Bersih</span><strong>${formatRupiah(m.cashFlow)}</strong></div></div><div class="tabs no-print" style="margin:16px 0"><button class="tab-button ${tab==='sales'?'is-active':''}" data-report-tab="sales">Penjualan</button><button class="tab-button ${tab==='purchases'?'is-active':''}" data-report-tab="purchases">Pembelian</button><button class="tab-button ${tab==='buybacks'?'is-active':''}" data-report-tab="buybacks">Buyback</button><button class="tab-button ${tab==='expenses'?'is-active':''}" data-report-tab="expenses">Pengeluaran</button></div><article class="card"><div class="card__head"><div><h3>${tabTitle()}</h3><p>${formatDate(startDate)} — ${formatDate(endDate)}</p></div></div><div class="card__body card__body--flush" id="report-table">${reportTable()}</div></article></section>`;
container.querySelector('#apply-report').addEventListener('click',async event=>{const button=event.currentTarget;setButtonLoading(button,true,'Memuat…');startDate=new Date(`${container.querySelector('#report-start').value}T00:00:00`);endDate=new Date(`${container.querySelector('#report-end').value}T23:59:59.999`);tab=container.querySelector('#report-tab').value;try{await loadData();draw(container)}catch(error){toast(getErrorMessage(error),'error');setButtonLoading(button,false)}});container.querySelectorAll('[data-report-tab]').forEach(btn=>btn.addEventListener('click',()=>{tab=btn.dataset.reportTab;draw(container)}));container.querySelector('#print-report').addEventListener('click',()=>window.print());container.querySelector('#export-report').addEventListener('click',exportCurrent);bindVoid(container)}
function tabTitle(){return({sales:'Rincian Penjualan',purchases:'Rincian Pembelian',buybacks:'Rincian Buyback',expenses:'Rincian Pengeluaran'}[tab])}
function reportTable(){if(tab==='sales')return salesTable();if(tab==='purchases')return purchaseTable();if(tab==='buybacks')return buybackTable();return expenseTable()}
function salesTable(){if(!sales.length)return tableEmpty('Tidak ada penjualan pada periode ini.');return `<div class="table-wrap"><table class="data-table"><thead><tr><th>Waktu</th><th>Invoice</th><th>Pelanggan</th><th>Barang</th><th>Pembayaran</th><th>Total</th><th>Status</th><th>Kasir</th><th></th></tr></thead><tbody>${sales.map(item=>`<tr><td>${formatDateTime(item.createdAt)}</td><td><strong>${escapeHTML(item.invoiceNo)}</strong></td><td>${escapeHTML(item.customerName||'Pelanggan Umum')}</td><td>${(item.lines||[]).length} jenis • ${formatNumber(sum(item.lines||[],l=>l.weightGrams))} gr</td><td>${escapeHTML(item.paymentMethod||'—')}</td><td><strong>${formatRupiah(item.grandTotal)}</strong></td><td>${item.status==='void'?badge('Dibatalkan','danger'):badge('Selesai','success')}</td><td>${escapeHTML(item.createdByName||'—')}</td><td>${canManage()&&item.status!=='void'?`<button class="button button--danger button--sm no-print" data-void-sale="${item.id}">Batalkan</button>`:''}</td></tr>`).join('')}</tbody></table></div>`}
function purchaseTable(){if(!purchases.length)return tableEmpty('Tidak ada pembelian pada periode ini.');return `<div class="table-wrap"><table class="data-table"><thead><tr><th>Waktu</th><th>Nomor</th><th>Pemasok</th><th>Rincian</th><th>Status</th><th>Total</th><th>Petugas</th></tr></thead><tbody>${purchases.map(item=>`<tr><td>${formatDateTime(item.createdAt)}</td><td>${escapeHTML(item.purchaseNo)}</td><td>${escapeHTML(item.supplierName||'—')}</td><td>${(item.lines||[]).length} produk • ${formatNumber(sum(item.lines||[],l=>l.weightGrams))} gr</td><td>${badge(item.paymentStatus||'—',item.paymentStatus==='Lunas'?'success':'warning')}</td><td><strong>${formatRupiah(item.total)}</strong></td><td>${escapeHTML(item.createdByName||'—')}</td></tr>`).join('')}</tbody></table></div>`}
function buybackTable(){if(!buybacks.length)return tableEmpty('Tidak ada buyback pada periode ini.');return `<div class="table-wrap"><table class="data-table"><thead><tr><th>Waktu</th><th>Nomor</th><th>Pelanggan</th><th>Kadar</th><th>Berat</th><th>Harga/Gram</th><th>Total</th><th>Petugas</th></tr></thead><tbody>${buybacks.map(item=>`<tr><td>${formatDateTime(item.createdAt)}</td><td>${escapeHTML(item.buybackNo)}</td><td>${escapeHTML(item.customerName||'—')}</td><td>${item.karat}K</td><td>${formatNumber(item.netWeight)} gr</td><td>${formatRupiah(item.buyRate)}</td><td><strong>${formatRupiah(item.totalPayout)}</strong></td><td>${escapeHTML(item.createdByName||'—')}</td></tr>`).join('')}</tbody></table></div>`}
function expenseTable(){if(!expenses.length)return tableEmpty('Tidak ada pengeluaran pada periode ini.');return `<div class="table-wrap"><table class="data-table"><thead><tr><th>Tanggal</th><th>Nomor</th><th>Kategori</th><th>Deskripsi</th><th>Pembayaran</th><th>Total</th><th>Petugas</th></tr></thead><tbody>${expenses.map(item=>`<tr><td>${formatDate(item.expenseDate)}</td><td>${escapeHTML(item.expenseNo)}</td><td>${escapeHTML(item.category)}</td><td>${escapeHTML(item.description)}</td><td>${escapeHTML(item.paymentMethod)}</td><td><strong>${formatRupiah(item.amount)}</strong></td><td>${escapeHTML(item.createdByName||'—')}</td></tr>`).join('')}</tbody></table></div>`}
function bindVoid(container){container.querySelectorAll('[data-void-sale]').forEach(btn=>btn.addEventListener('click',async()=>{const sale=sales.find(item=>item.id===btn.dataset.voidSale);const reason=prompt(`Alasan pembatalan ${sale.invoiceNo}:`);if(!reason)return;if(!await confirmDialog({title:'Batalkan Transaksi',message:'Stok produk akan dikembalikan otomatis. Lanjutkan?',confirmText:'Batalkan Transaksi',danger:true}))return;try{await voidSale(sale.id,reason);toast('Transaksi dibatalkan dan stok dikembalikan.');await loadData();draw(container)}catch(error){toast(getErrorMessage(error),'error')}}))}
function exportCurrent(){let rows=[];if(tab==='sales')rows=sales.map(item=>({Waktu:formatDateTime(item.createdAt),Invoice:item.invoiceNo,Pelanggan:item.customerName,Metode:item.paymentMethod,Subtotal:item.subtotal,Diskon:item.discount,Pajak:item.tax,Total:item.grandTotal,Status:item.status,Kasir:item.createdByName}));if(tab==='purchases')rows=purchases.map(item=>({Waktu:formatDateTime(item.createdAt),Nomor:item.purchaseNo,Pemasok:item.supplierName,Total:item.total,Status:item.paymentStatus,Petugas:item.createdByName}));if(tab==='buybacks')rows=buybacks.map(item=>({Waktu:formatDateTime(item.createdAt),Nomor:item.buybackNo,Pelanggan:item.customerName,Karat:item.karat,Berat:item.netWeight,Harga:item.buyRate,Total:item.totalPayout,Petugas:item.createdByName}));if(tab==='expenses')rows=expenses.map(item=>({Tanggal:formatDate(item.expenseDate),Nomor:item.expenseNo,Kategori:item.category,Deskripsi:item.description,Total:item.amount,Metode:item.paymentMethod,Petugas:item.createdByName}));if(!rows.length)return toast('Tidak ada data untuk diekspor.','warning');downloadCSV(`laporan-${tab}-${dateInputValue(startDate)}-${dateInputValue(endDate)}.csv`,rows)}
